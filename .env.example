# ============================================================================
# laravel-rebel-sessions — environment variables
# ============================================================================

# Default number of days a remembered device stays trusted (callers may override).
REBEL_SESSIONS_DEVICE_TRUST_DAYS=30
